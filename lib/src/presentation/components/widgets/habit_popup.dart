import 'package:flutter/material.dart';

class HabitPopup extends StatelessWidget {
  const HabitPopup({super.key});

  @override
  Widget build(BuildContext context) {
    return Padding(
      padding: EdgeInsetsGeometry.symmetric(horizontal: 24),
    );
  }
}